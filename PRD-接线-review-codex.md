# PRD-研究系统接线-v0 红队评审（Codex）

- 评审日期：2026-07-24
- 评审对象：`PRD-研究系统接线-v0.md`
- 参照基线：`flows/out/round-review-v1.5-codex.md`、`SOP.md` v1.1、
  `flows/ROADMAP-v1.5.md`、预注册回测协议 v3/v3a
- 评审目标：不是替四方向补强叙事，而是寻找足以推翻“现在应该接线”及各 MVP
  价值主张的反例
- 严重度：**高**＝进入实现前必须修；**中**＝可以保留为待验证假设，但不得按现文
  宣称或验收；**低**＝表述或操作精度问题

## 总裁决

**需修改后重审。**

草案抓到了真实问题，但尚未证明“把这些 skill 接成统一系统”优于当前的手工调用 +
事件触发监测。四方向中，只有方向三的**业务需要**有既有证据支持；“产品化后有
增量价值”仍未证。方向一、二有纪律价值假设，方向四连要回答的问题都超出了现有
设计的识别能力。

进入实现前至少要修复四个阻断项：

1. 增加“不接线、维持现状”的对照基线和停止门；
2. 把统一 `claim` 拆成不可变主张、证据判定、监测规则、生命周期事件，不能用一个
   混合字段体系承载；
3. 重写方向一、二、三的成功判据；当前三个判据分别可被运气、分母操作和不可完成的
   全称检验救活；
4. 删除方向四当前阶段的“系统让判断变好”目标；观察性分桶即使 `n≥30` 也不能给出
   因果结论。

## 一、证据状态总表

| 方向 | 草案价值主张 | 当前证据状态 | 红队裁决 |
|---|---|---|---|
| 一：叙事缺口 | 缺口清单能识别真正风险点 | 溯源/台账能提高可审计性有单项目实证；“缺口能预测反转”未证 | **部分已证，核心价值未证** |
| 二：预注册登记簿 | 统一登记能提高纪律并可持续运行 | 回测协议的预注册范式已完成设计与盲审；跨 skill、连续四周、每日扫描均未运行 | **方法有旁证，接线价值未证** |
| 三：事件哨兵 | 事件触发能降低漏更并形成时间差 | R5/R7 事件触发维护方式已有明确依据；海关收割例程和图增量更新仍列为未验证 | **需要已证，自动接线增量未证** |
| 四：判断复盘 | 分桶能回答系统是否让判断变好 | 预注册流程范式已设计；交易登记摩擦、样本捕获和因果识别均未证 | **核心主张不成立** |

这里必须区分“原工序存在价值”“需要维护”“接线能提升价值”三件事。草案多处以前
两者替第三者背书。

---

## 二、总前提

### F01｜“接线优于维持现状”没有基线证据

- **位置**：PRD:7–11、25、104–112
- **问题**：文中列出“无自动传导、依赖手动想起”便直接进入统一接线方案，但没有
  记录现状下的漏触发率、人工耗时、误触发率、判断漏登记率，也没有证明这些损失大于
  接线带来的登记、维护、告警和纠错成本。“不接线、继续手动”被排除在比较之外。
- **严重度**：**高**
- **建议**：新增 Gate 0：维持现有 R5/R7 事件触发和分支 F 月度留痕，在预先限定的
  一个完整发布周期内记录 `{应处理事件, 实际发现时间, 是否漏掉, 人工分钟数, 是否
  产生有效更新}`。任何接线 MVP 必须在相同口径下与该基线比较；若只减少几分钟但
  增加持续维护，则维持手工即为合格终态。

### F02｜“防御价值已验证”被从单项目外推到整个技能系统

- **位置**：PRD:10–14
- **问题**：彩排验证的是供应链图谱在证据回溯、等级约束和错误拦截上的防御价值，
  不是五套框架、sector-scan、watchlist 与交易计划接线后的系统价值。现文没有限定
  证据适用域，违反“单例不得全称化”。
- **严重度**：**中**
- **建议**：改成“供应链彩排中，证据纪律的防御价值已有单域实证；跨 skill 传导、
  持续登记和决策增量均未证”。把每个新方向的证据状态单列，不用“系统整体”承接。

### F03｜“统一契约或各自造轮子”是假二分

- **位置**：PRD:25
- **问题**：四方向可以先用最小事件记录和稳定文件引用对接，不必先造一个跨所有
  claim 类型的总契约。更便宜的替代是：方向三工单只引用受影响的边/判例，方向一
  缺口清单只引用原始叙事和事实判定；重复字段在两次真实运行后再蒸馏。草案回避了
  这一中间方案。
- **严重度**：**高**
- **建议**：删除“共享地基先行”。采用“先跑两个窄流程 → 从实际记录中抽公共字段
  → 再决定是否统一”的顺序，保持本项目“SOP 从执行记录长出”的设计原则。

---

## 三、统一 claim 契约

### F04｜`证据等级`混合了三套不可互换的分类

- **位置**：PRD:21–23
- **问题**：`实`是关系边的准入类型，`A/B/C`是解匿判定等级，`传闻/未核`是来源或
  核验状态；fact-provenance-tracer 另有“直接证实/间接证实/部分证实/无法证实/
  证伪/公开不可验证”六级结论。这些类别既不互斥也不在同一序数轴上。“alpha 主张
  一律为 C 级”又把解匿等级借给预测假设，语义进一步漂移。
- **严重度**：**高**
- **建议**：至少拆为：
  `source_tier`（来源层级）、`verification_verdict`（事实核查结论）、
  `domain_grade`（可选，边/解匿等领域专用等级）、`confidence_basis`（理由）。
  禁止跨域复用字母等级。

### F05｜契约缺少可复现所需的最小数据血缘

- **位置**：PRD:18–23
- **问题**：只有“来源(skill/人/研报)”不能重建当时信息集。缺少来源文件/URL、
  锚点、抓取或生成时间、版本/hash、主张适用对象与范围、截至时点、操作者、
  单位/口径、证据引用和修订链。同一 skill 下次运行后，旧 claim 将无法证明当时
  看到了什么。
- **严重度**：**高**
- **建议**：不可变主张最小字段应含
  `{claim_id, created_at, as_of, author, subject/scope, claim_text, claim_kind,
  source_artifact_id, source_version_or_hash, source_locator, evidence_refs}`。
  预测类再选填 `{horizon, comparator, falsifier, monitor_rule_id}`，不要让一个
  “日期”承担创建、适用、观测和截止四种时间。

### F06｜统一三要素对事实/逻辑/配置主张既过度又不足

- **位置**：PRD:21–23、47–55
- **问题**：事实主张未必有阈值和期限，逻辑链可能需要前提集合与推导，配置主张
  本质是策略规则；强迫四类都填“阈值+期限+可推翻条件”会诱发伪精确。反过来，
  真正可监测的预测又缺数据源、操作符、单位、频率、容差和缺失值处置。
- **严重度**：**高**
- **建议**：先定义类型特定 schema。把监测规则独立为
  `{variable, operator, threshold, unit, data_source, cadence, window, tolerance,
  missing_data_rule}`；只有预测/配置中需要监测的部分引用它。无法机械化的逻辑
  主张保留为可审计文本，不为接线而硬造阈值。

### F07｜“JSONL 追加不回改”与可变状态没有事件模型

- **位置**：PRD:18、22、56、61–62、119
- **问题**：`状态`会从开放变为触发/证实/证伪/过期；若原行不改，必须追加状态
  事件并定义当前态如何归并。现文却把主张与当前状态放在同一个对象中，也没有
  `event_id`、`supersedes`、生效时间、纠错原因和冲突处置。实际执行只能二选一：
  偷改旧行，或堆出无法确定当前态的重复 claim。
- **严重度**：**高**
- **建议**：拆成两张逻辑表：
  1. `claims.jsonl`：不可变主张定义；
  2. `claim-events.jsonl`：`created/triggered/verified/falsified/expired/corrected`
     事件，字段含 `{event_id, claim_id, event_type, observed_at, effective_at,
     actor, evidence_refs, supersedes_event_id, reason}`。
  当前状态是可重建视图，可以覆盖生成；原始主张与事件才追加不改。

### F08｜“账本一律追加不回改”把审计纪律错误扩展到所有派生产物

- **位置**：PRD:61–62、119；对照 SOP:S3.3、版本纪律
- **问题**：不可改应约束原始证据、冻结协议和历史事件，不应约束所有“账本类文件”。
  现有 SOP 的解匿流程明确允许原槽位行升级为推断边；项目也使用“旧版冻结 +
  新快照/勘误”而非无限追加每日全量状态。个人使用若把每次扫描结果都永久追加，
  噪声和维护成本会很快超过审计收益。
- **严重度**：**中**
- **建议**：改为“原始记录不可静默改写；纠错以追加事件或新版本完成；当前视图和
  派生表可重建覆盖”。为压缩、归档、重建和 schema 迁移写明规则。

### F09｜缺角色、权限、幂等与状态转移，尚不能交给实现

- **位置**：PRD:16–25、50–56、75–77
- **问题**：“skill/人/模型”均可读写同一本账，但未定义谁能创建、触发、证实、
  证伪、纠错和批准台账更新；也没有重复运行、并发写入、部分失败、重试和重复事件
  的处置。个人系统仍至少有“模型提议—用户批准—系统落账”三个责任位。
- **严重度**：**高**
- **建议**：补一条最小闭环：
  `候选生成 → schema 校验 → 用户批准/驳回 → 追加事件 → 派生视图重建`；
  每步明确 actor、输入、输出、失败态和幂等键。未经批准的模型输出只能处于
  `proposed`，不得直接修改事实台账或触发交易动作。

---

## 四、方向一：叙事—披露缺口审计器

### F10｜价值证据只有“能审”，没有“缺口能预测风险”

- **位置**：PRD:31–42
- **问题**：彩排和 fact-provenance 方法支持“可以把主张追到证据”，不支持
  “无公开披露支撑的叙事更可能反转”。研报/公众号样本的选取、主流叙事定义及
  回声室去重也未写定。
- **严重度**：**中**
- **建议**：把价值主张拆成两级：MVP 只验证覆盖率、血缘完整性和人工成本；
  “缺口是否富集后续修正”另做前瞻研究，冻结全部入样叙事、对照组和判定窗口后
  全量报告。

### F11｜两个板块不是最小 MVP

- **位置**：PRD:39–40
- **问题**：光模块一次全流程加第二个无台账板块，同时改变行业、数据基础和处理
  模式，既扩大范围又无法定位失败原因。光模块台账还是高证据密度样本，成功不代表
  无台账行业可行。
- **严重度**：**中**
- **建议**：最小试验只取一份已冻结的光模块扫描输出和其中预先列出的 3–5 条承重
  主张，手工生成缺口清单并记录每条成本、不可核比例和是否改变后续核查优先级。
  通过操作性门后，第二行业是独立外部有效性试验，不属于同一 MVP。

### F12｜“三个月内至少一次反转”把清单价值与运气混同

- **位置**：PRD:41–42
- **问题**：只要标很多缺口，碰到一次反转即可过线；反转也可能来自随机事件而非
  缺口。反之，真正高风险的主张三个月内没有新披露，会被误判为清单无用。没有
  分母、支持组对照、披露机会集、反转定义、冻结时点和“不足以判定”状态，结果可被
  事后解释救活。
- **严重度**：**高**
- **建议**：MVP 不用未来反转作成功门。若以后检验风险识别，必须在结果出现前冻结
  全部“缺口/非缺口”样本、可观察披露窗口和反转判定器；期末全量报告
  `{证伪, 支持, 无新披露, 口径不可比}`，比较两组修正率及不确定区间，不得以
  “至少命中一次”验收。

### F13｜“不做”清单没有禁止把披露缺失当成负面证据

- **位置**：PRD:35–43
- **问题**：虽写“缺口≠虚假”，仍未禁止将“公开不可验证”排序成低质量公司、
  把多个转载当独立共识、在事件发生后倒选叙事、自动对外发布可能构成指控的表述，
  也未处理付费/登录墙和来源授权。
- **严重度**：**高**
- **建议**：增加红线：公开缺失不构成证伪；转载链按同一源计数；叙事集合在核验前
  冻结；公开不可验证与证伪分列；缺口清单仅供核查排序，不自动生成交易/公司质量
  结论；登录墙标不可验证，不绕行、不转存无权保存的全文。

---

## 五、方向二：全系统预注册登记簿

### F14｜“强制降解”会把框架判断压成伪精确

- **位置**：PRD:47–55
- **问题**：并非每个框架输出都是可执行预测。有些是事实、情景、约束或尚待观察的
  逻辑。强迫每条判断带阈值、期限、可推翻条件，会诱发任意阈值；这正是回测协议
  要求登记“阈值无独立依据/历史污点”所防的自由度。
- **严重度**：**高**
- **建议**：只登记会改变监测、配置或退出条件的“决策承重 claim”。其他输出保留
  原文引用。所有阈值增加 `{依据, 选定时已见信息, 适用范围}`，无依据可登记但必须
  标“约定”，不得因已填表而升格。

### F15｜两源同时接入不是最小验证

- **位置**：PRD:57–58
- **问题**：同时接 spread-macro-watch 和 market-constraint-auditor，会混入两种
  输出结构和重复/冲突主张，无法判断摩擦来自契约还是连接器。
- **严重度**：**中**
- **建议**：先选结构最接近契约的一个源，手工登记一个完整输出周期；只有当
  `候选判断→接受/拒绝→触发→关闭`全链路闭合后，第二源才用于验证通用性。

### F16｜80% 完整率既违反准入规则，也可被分母操纵

- **位置**：PRD:53、59–60
- **问题**：53 行说“三要素缺一不可”，59 行却允许 20% 的开放 claim 不完整。
  只统计“开放”还可通过不入账、提前过期或改状态缩小分母；连续四周也只是存活时间，
  不证明捕获了应登记判断。
- **严重度**：**高**
- **建议**：拆成三个不可互相替代的门：
  1. `schema_valid / admitted = 100%`；
  2. `registered / eligible_candidate_judgments` 全量报告，候选分母在看结果前冻结；
  3. 报告创建、更新、关闭的人工分钟数和放弃原因。
  不完整项进入 `rejected` 并保留原因，不能混在开放账中。

### F17｜登记簿的危险失败模式未被禁止

- **位置**：PRD:50–62
- **问题**：现文只禁止排名和删除，未禁止重复 claim 造成告警洪水、同义/冲突主张
  静默合并、数据源失效时误触发、触发后自动执行交易、框架输出遗漏却不计分母，
  以及旧 skill 版本覆盖新版本。
- **严重度**：**中**
- **建议**：增加去重但不吞冲突、缺失数据=`unknown`而非安全/触发、触发只生成
  待审工单、所有候选输出计入覆盖率分母、来源版本固定、告警预算和熔断机制。

---

## 六、方向三：披露事件哨兵

### F18｜“四类通道全部验证”与本地记录冲突

- **位置**：PRD:69–74、108；对照 ROADMAP:27–29、SOP:340–351
- **问题**：路线图把 R5 的“数据缺口”标已证、研究增量标未证，把 R7 整体标未证；
  SOP 附录B又明确“收割库 S07 海关月度未走”“图更新/增量运行未验证”。年报/IPO
  的文档获取有实测，不等于日历监测、变更检测和自动重开通道均已验证。
- **严重度**：**高**
- **建议**：逐事件源拆成
  `{原始来源可达, 发布检测已运行, 工单生成已运行, 增量更新已运行, 价值已验证}`
  五列。当前只能按实际记录填状态，不得用“通道验证”总括。

### F19｜海关 MVP 与现有分支 F 义务重合，无法识别接线增量

- **位置**：PRD:78–79
- **问题**：无论是否开发哨兵，分支 F 已要求每月按事件触发采集并留痕。一次成功
  入库只能证明既定义务被执行，不能证明新增日历/工单系统比现有手工提醒更好。
- **严重度**：**中**
- **建议**：以现有手工流程为主路径，哨兵先跑 shadow mode；预注册比较
  `{是否发现, 检测延迟, 工单正确率, 人工分钟数, 误报数}`。若不能显著降低漏更或
  操作成本，就不产品化，继续手工。

### F20｜“早于任一主流研报”是不可完成的全称检验

- **位置**：PRD:80–81
- **问题**：无法穷尽“任一主流研报”，也未定义主流、同一披露、首次引用、发布时间
  与时区。即使偶然更早，也只证明一次速度，不证明更新正确、有决策价值或由哨兵
  造成。
- **严重度**：**高**
- **建议**：主成功门改为内部 SLA：
  `官方发布→检测→工单→人工确认→台账新版本`各时间戳齐全，在事前约定时限内完成，
  影响对象抽检正确，且零未经批准的事实升级。若要观察外部时间差，只能预先冻结
  有限媒体/研报清单和检索截止时点，作为描述性旁证，禁止写“任一”。

### F21｜事件更正、重复、漏报和自动更新风险未设红线

- **位置**：PRD:75–82
- **问题**：“事件到达→台账更新”没有处理官方更正/撤回、同事件多次发布、来源暂时
  不可达、日历漂移、空更新、历史补发、模型误判影响范围等状态，也未禁止模型直接
  升级边等级或改写冻结判例。
- **严重度**：**高**
- **建议**：事件状态至少为
  `expected/detected/deduplicated/reviewed/applied/no-op/corrected/failed`；
  原始文件按 hash 去重并保留更正版；模型只提议受影响对象，人工批准后走 SOP；
  无变化也以 `no-op` 关闭，失败和漏报进入分母，不能只统计成功工单。

---

## 七、方向四：判断追踪与复盘闭环

### F22｜观察性分桶不能回答“系统让判断变好”

- **位置**：PRD:86–95
- **问题**：是否使用证据核查不是随机分配：更难、更大仓位或更重要的交易更可能
  调用更多 skill；市场阶段、资产类别、持有期和执行质量也同时变化。按“用了哪些层”
  分桶得到的胜率/赔率差异有严重选择偏差。`n≥30`只增加样本数，不自动解决混杂，
  因而不能回答因果问题。
- **严重度**：**高**
- **建议**：把目标降为“描述登记完整性与结果分布”，不得写“让判断变好”。若未来
  要估计增量价值，需预先定义可比样本、处理分配、风险调整基准、独立性单位和分析
  计划；在此之前只能报告相关性，不能归因于系统或某层 skill。

### F23｜`n<30` 前的登记摩擦和选择性漏登被低估

- **位置**：PRD:89–98
- **问题**：每笔不仅要“登记”，还要冻结信息集、定位多个 skill 版本、写三门、
  选基准、记录是否执行、处理止损/止盈/未成交/部分成交并到期关闭。连续 10 笔可能
  跨很久；操作者可跳过难登记或亏损计划，最后只留下整洁样本。`10 笔闭环`衡量合规，
  不等于“摩擦低到可持续”。
- **严重度**：**高**
- **建议**：先做 3–5 个**影子登记**的摩擦试验，不改变实际下单；分母是所有符合
  事前资格的计划，不是成功入账计划。预注册用户可接受的
  `{单笔创建分钟数, 更新分钟数, 关闭分钟数, 捕获率, 放弃率}`停止门；不过门即砍，
  不等凑够 10 笔。

### F24｜“零事后改判据”会鼓励隐藏真实错误

- **位置**：PRD:97–98
- **问题**：数据录入错误、单位错误和来源更正客观存在。绝对禁止修订会使操作者
  要么保留明显错误，要么静默改写。真正要禁止的是揭盲后让旧判据追溯生效，不是
  禁止透明纠错。
- **严重度**：**中**
- **建议**：允许追加 `amendment`，保留原文、原因、时间和当时是否已见结果；
  揭盲前修订只对后续生效，揭盲后修订不得用于原计划裁决，原计划按预注册规则判
  `protocol_deviation` 或 `data_error`。

### F25｜交易试验的“不做”清单仍有高风险空白

- **位置**：PRD:92–100
- **问题**：未禁止只登记已执行/盈利计划、用相关交易当独立样本、为凑样本提高
  交易频率或风险、忽略未成交与交易成本、用事后市场数据补信息集、把描述性分桶
  用于评估个人能力或自动调整仓位。
- **严重度**：**高**
- **建议**：补充：资格事件全量登记；同一 thesis 的多笔交易按同一簇处理；未成交/
  取消/部分成交保留；冻结信息集后不得补入未来信息；不得为试验改变风险预算；
  未经独立设计不得据此评价系统、skill 或操作者能力，更不得自动调仓。

---

## 八、优先级与依赖

### F26｜“claim 契约半天完成、四方向共同依赖”没有成立

- **位置**：PRD:104–112
- **问题**：方向三海关留痕可直接沿用既有日志和数据表，不依赖通用 claim；方向一
  可用叙事原文+溯源判定；方向四的信息集快照也可先引用冻结文件。反而通用契约要
  解决版本、状态、证据分类、幂等、纠错和迁移，不可能由“字段写完”证明半天完成。
- **严重度**：**高**
- **建议**：取消“共享地基先行”和半天估算。先执行窄流程并记录真实对象；只有至少
  两个流程出现同一字段/状态摩擦后，才抽象公共契约。方向三不应等待契约。

### 修正后的执行序

1. **Gate 0：什么都不接。**维持当前分支 F 月度留痕 + R5/R7 事件触发，取得手工
   基线；这是正式候选，不是“失败前状态”。
2. **方向三 shadow MVP。**只选海关一个事件源，先检测和生成候选工单，不自动更新
   台账；与手工基线比较。若无漏报/成本增量，停在手工态。
3. **方向二单源 MVP。**仅在方向三或另一真实流程已经暴露跨 skill 登记摩擦时，
   用一个源试跑不可变 claim + event ledger；不先接两个源。
4. **方向一一次性手工试验。**先验证缺口清单是否以可接受成本改变核查优先级；
   未证明前不自动附着到每次 sector scan。
5. **方向四从当前路线删除。**可保留一个轻量决策日志，但不得命名为系统效果试验；
   只有出现自然积累的、完整捕获的可比样本和独立识别设计后，才另立 PRD。

这不是简单保留“三→二→一→四”。关键修正是：增加“不接线”基线；方向三先不依赖
通用契约；方向二从真实运行中蒸馏契约；方向四不是“最后做”，而是**现在砍掉**。

---

## 九、全局红线与 PRD 自身纪律

### F27｜红线把“纪律渗透与时间差”写成已证价值，仍属越级

- **位置**：PRD:114–117
- **问题**：纪律完整率是过程指标，时间差是速度指标；二者都未自动等于研究质量或
  决策价值。“系统当前唯一可证的价值路径”既无出处，又是全称化。方向四出数也不能
  自动把 alpha 从 C 级升级，因为其设计没有因果识别。
- **严重度**：**高**
- **建议**：改成“本 PRD 首轮只检验可审计性、漏触发、延迟和人工成本；决策价值与
  alpha 均不在首轮可证范围”。删除“唯一”和跨域 C 级。

### F28｜全局红线不完备

- **位置**：PRD:114–120
- **问题**：缺少以下系统级禁区：自动交易/调仓；模型未经批准改事实台账；把
  未披露当证伪；付费墙与授权内容越权保存；来源更正不传播；重复/循环来源当独立
  证据；事后改分母、样本或判据；缺失数据默认为安全；自动对外发布未核指控；
  旧版本静默覆盖。
- **严重度**：**高**
- **建议**：将上述禁区写入全局红线，并给每条配置机械检查或人工审批位。红线不能
  只管措辞和沉没成本，还要管数据权限、状态变更和决策动作。

### F29｜PRD 自身存在无出处断言和全称化

- **位置**：PRD:25、69、94、107–109、116–117
- **问题**：包括“有契约就等于接线”“四类全部验证”“唯一重要的问题”“claim
  契约半天”“方向二无外部依赖”“唯一可证的价值路径”。其中“四类全部验证”已被
  本地 SOP/路线图反证，其余均无执行记录支撑。草案因此违反自己继承的“证据分级、
  单例不得全称化”纪律。
- **严重度**：**高**
- **建议**：所有此类句子改成带证据状态的假设；新增“主张—本地出处—已运行/
  仅设计/未验证”表。没有日志的跨 skill 机制只能写“待试”，不能写成地基、依赖或
  已证价值。

---

## 十、相对“不接线”的边际价值

基线不是零维护，而是上一轮已经确定的状态：停止常态化人工扩展，保留分支 F 月度
留痕、年报/IPO/ggzy 等披露事件触发。

| 排名 | 方向 | 相对基线的边际价值 | 主要理由 | 当前动作 |
|---:|---|---|---|---|
| 1 | **方向三** | **中，但自动化增量未证** | 事件触发本身与既有义务一致；潜在收益是少漏更、降延迟。手工提醒可能已取得大部分价值 | 只做单源 shadow，对照手工 |
| 2 | **方向二** | **中低** | 可提高可追责性，但会引入持续登记与告警成本；预注册范式有旁证，跨 skill 可持续性未证 | 单源、按真实摩擦抽契约 |
| 3 | **方向一** | **低** | 一次性人工溯源已能取得主要防御价值；自动附带每次扫描的成本和风险富集能力均未证 | 先做一次手工试验 |
| 4 | **方向四** | **当前为负** | 登记成本最高，样本慢、选择偏差强；现设计不能回答其宣称的因果问题 | **砍掉当前方向** |

### 我会砍掉哪个

**砍掉方向四的当前方案。**

理由不是“样本还不够”这么简单，而是即使自然积累到 30 笔，现有观察性分桶仍不能
回答“系统让判断变好”。继续登记会先支付最高摩擦，再得到一个无法支持核心主张的
结果。保留轻量决策日志可以，但它属于个人复盘，不应伪装成系统效果试验。

## 最低重审清单

再次送审前，PRD 至少应出现以下改动：

1. Gate 0 手工基线、比较指标和停止条件；
2. `claim`/`claim_event` 分离，证据三维拆分，类型特定字段、角色与状态机齐全；
3. 四方向各自的“已证/未证/本轮只验证什么”；
4. 方向一删除“三个月至少一次反转”；
5. 方向二删除“开放 claim 80%”并固定候选分母；
6. 方向三删除“早于任一主流研报”，改内部 SLA + 有限外部样本旁证；
7. 方向四从实现路线删除或彻底降格为摩擦观察，不再宣称系统效果；
8. 补全全局红线，删除 PRD 自身的无出处全称断言。

完成这些修改后，可以重审；在此之前不建议进入接线实现。
