# PRD 草案 v0：个人研究操作系统"接线"计划（四方向）

日期：2026-07-24 ｜ 状态：草案，未评审 ｜ 前置：workflow-rehearsal 彩排完成（SOP v1.1）

## 背景与问题

现有资产分三条流水线：事实层（台账/溯源/骨肉分离）、框架层（钢印/聚义厅/老钱/
Spread/体感五套核对表）、状态层（sector-scan/主线/轮动/主导约束/watchlist）。
问题：**层间无自动传导**——信号不触发证据核查，观点不留可核对的登记，事件不
触发更新，判断没有事后追责。系统整体只有"防御价值已验证"，且防御力依赖使用者
每次手动想起来调用。

**总设计原则（继承彩排纪律）**：主张与核对器分离；一切登记预注册（先写判据后看
结果）；证据分级承重；诚实边界（"不可知"是正式输出）；单例不得全称化。

## 统一数据契约（四个方向共享的地基）

定义一个跨 skill 的 `claim`（主张）对象，落盘为 JSONL 账本：

```
claim_id | 日期 | 来源(skill/人/研报) | 主张原文 | 类型(事实/逻辑/预测/配置)
| 证据等级(实/A/B/C/传闻/未核) | 可推翻条件 | 触发变量与阈值 | 状态(开放/触发/证实/证伪/过期)
```

没有这个契约，四个方向各自造轮子；有了它，"接线"=不同 skill 读写同一本账。

---

## 方向一：叙事-披露缺口审计器（信号层 → 事实层）

**一句话**：sector-scan 或主线识别报出热点板块后，自动把该板块的主流叙事逐条
过证据台账，输出"共识与披露的缺口清单"。

- **触发**：sector-scan 输出买入级板块 / 主线识别输出当日主线。
- **动作**：①收集该板块 3-5 条承重叙事（研报+公众号，wewerss 已有信源）；
  ②每条走 fact-provenance-tracer 六级判定 + （已有台账的行业）查 edges.csv；
  ③输出：叙事 → 披露支撑等级 → 缺口标注（"此论断无公开披露支撑，依赖渠道信息"）。
- **产出物**：`缺口清单.md`（板块级，随扫描结果附带）。
- **MVP**：光模块板块（台账现成）跑通一次全流程；第二个板块验证无台账时
  纯溯源模式是否成立。
- **成功判据（可证伪）**：一次扫描信号附带的缺口清单中，标注"无披露支撑"的
  叙事在 3 个月内出现至少一次公开证伪或披露反转（说明缺口清单标的是真风险点）。
- **不做**：不因缺口直接给出做空/回避建议；缺口≠虚假，只是信心分级。

## 方向二：全系统预注册登记簿（框架层 → 状态层）

**一句话**：五套框架 skill 和主导约束审计每次输出的判断，强制降解成
claim 对象登记入账，watchlist-tracker 从"跨资产阈值追踪器"升级为全系统登记簿。

- **触发**：任何框架 skill 运行结束时，末尾追加"登记段"：本次核对产生了哪些
  带触发变量的判断（如聚义厅"负 gamma 地形转正即撤"、Spread"论点 T3 看
  某变量突破"）。
- **动作**：判断写入 claim 账本（预注册：阈值+期限+可推翻条件缺一不可，
  学 backtest-protocol 六要素的收敛版三要素）；watchlist-tracker 每日扫描
  时合并跨 skill 的全部开放 claim。
- **产出物**：`claims.jsonl` + 每日触发状态表（触发/接近/安全/过期）。
- **MVP**：先接 spread-macro-watch（其论点→变量结构已经最接近 claim 契约）
  和 market-constraint-auditor 两个源。
- **成功判据**：连续 4 周，账本里 ≥80% 的开放 claim 具备完整三要素（阈值/
  期限/可推翻条件）——衡量的是纪律渗透率，不是判断对错率。
- **不做**：不给 claim 打分排名；过期 claim 标过期不删除（账本不可回改，
  同台账纪律）。

## 方向三：披露事件哨兵（事件 → 事实层更新）

**一句话**：把彩排的 R5/R7 事件触发机制产品化——盯披露日历，事件落地自动
触发台账更新与判例重开检查。

- **事件源**（首批四类，全部已在彩排中验证过信息通道）：
  ①海关月度批次（每月 20 日前后 → 分支 F 留痕 + 序列更新）；
  ②年报/10-K 季（Lumentum FY2026 约 2026-09；A 股 2027-03~04 → 披露史考古窗口、
  匿名化/实名化边界移动检测）；
  ③IPO 进程（联讯上会稿 → 判例#006 升级条件）；
  ④集采/中标公示（ggzy 新批次）。
- **动作**：事件到达 → 生成核查工单（哪些边受影响、哪些判例可重开、哪些
  claim 的触发变量被更新）→ 人工/模型执行 → 台账与判例库更新走既有 SOP。
- **产出物**：`事件日历.md` + 每事件一张核查工单。
- **MVP**：只做海关月度（最近的确定性事件：2026-08-20 前后 2026-07 批次）
  ——正好与回测分支 F 的逐月留痕义务合并，一鱼两吃。
- **成功判据**：一个披露窗口内，哨兵触发的台账更新早于任一主流研报引用同一
  披露（抢到过一次"时间差"，进攻假设的第一个检验点）。
- **不做**：不做全行业爬虫；事件源逐个手工接入，通道未验证的不上线。

## 方向四：判断追踪与复盘闭环（决策层 → 反馈）

**一句话**：把 trade_plan_builder 的每份交易计划当成一次预注册试验，用
backtest-protocol 的范式做事后判定，回答"这套系统到底有没有让判断变好"。

- **触发**：trade_plan_builder 生成计划时，强制附带：信息集快照（当时用了
  哪些 skill 的哪些输出）、预设判据（同回测三门：规格/事件命中/相对基准）、
  冻结时间戳。
- **动作**：计划到期或触发止损止盈 → 按预设判据机械判定（成功/失败/数据
  不足三态，禁止事后解释）→ 按"用了哪些层"分桶统计。
- **产出物**：`判断账本.md`——长期回答唯一重要的问题：**带证据核查的判断
  vs 不带的判断，胜率赔率有无可分辨差异**。
- **MVP**：纯登记模式跑 10 笔（不求统计显著，先验证登记摩擦是否低到可持续）。
- **成功判据**：第一阶段=连续 10 笔计划完成闭环判定且零事后改判据；
  第二阶段（≥30 笔后）=分桶差异可报告（无论方向）。
- **不做**：样本不足时不下"系统有效/无效"结论（n<30 只报登记状态）；
  永不把单笔盈亏归因于单个 skill。

---

## 优先级建议与依赖

```
共享地基: claim 契约 (半天)
   ├── 方向三 MVP (海关哨兵)   ← 最先做：有确定日程(8月下旬)、与分支F义务重合、零新通道
   ├── 方向二 MVP (两源登记)   ← 第二：纯纪律改造，无外部依赖
   ├── 方向一 MVP (光模块缺口清单) ← 第三：依赖台账，正好复用彩排资产
   └── 方向四 MVP (10笔登记)   ← 最后：依赖前三者产生的"信息集快照"才有内容
```

## 全局红线（对外承诺的反面）

- 四个方向的成功判据里**没有一个是"赚钱"**——系统当前唯一可证的价值路径是
  纪律渗透与时间差，alpha 主张在方向四出数前一律为 C 级假设；
- 任何方向的产出不得含"坐实/唯一/必然"类措辞（红队清单继承）；
- 账本类文件一律追加不回改；
- 每个方向 MVP 失败的处置预注册：记录摩擦、降级或砍掉，不追加沉没成本。
