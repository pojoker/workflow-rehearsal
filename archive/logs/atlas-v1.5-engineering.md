# 图谱工程路执行日志：Atlas v1.5

## 2026-07-24

- 动作前记录：开始承接 X2 前端分工。计划只读检查 `REHEARSAL.md`、现有日志格式、`flows/src/build_atlas_v14.py` 以及 `output/nodes.csv`、`output/edges.csv` 的结构与规模；不修改冻结的 v1.4 脚本和 HTML。

### [P4-X2-v1.5-01] 核对协议、基线与输入规模
- 意图: 确认日志格式、冻结边界、v1.4 实现基线以及 v1.5 输入规模。
- 动作: 读取 `REHEARSAL.md`、`flows/src/build_atlas_v14.py`、CSV 表头与样例；用 `wc -l` 核对行数；用 `rg` 查找图谱相关既有记录。
- 耗时: 0 次外部检索，约 2 分钟。
- 产出: 确认 CSV 为 161 个数据节点与 225 条边（含表头分别 162/226 行）；v1.4 构建脚本 486 行；v1.4 使用手工 `LAYER` 与四列横向布局，输入仍指同一对 CSV。
- 偏差: 首条临时日志未采用协议规定的六字段格式；现已补记为正式条目，后续按六字段记录。
- 摩擦: 并行检查输出被截断，需分段读取 v1.4 余下内容；无工具缺口。

- 动作前记录：下一步只读统计 `nodes.csv` 的全部类型值、`edges.csv` 的边等级值与端点规范化覆盖情况，并分段读取 v1.4 余下模板、布局、交互和自测代码，为最小差异实现做准备。

### [P4-X2-v1.5-02] 枚举分类空间与复用交互
- 意图: 穷举七层映射所需的类型值，确认新增等级与 v1.4 交互是否可直接复用。
- 动作: 统计 161 节点的 55 种 `类型`、225 边的 7 种 `边等级`；读取 v1.4 模板余下部分；检索 DSP 制度性盲区既有说明与版本线索；检查冻结文件及新目标文件的 git 状态。
- 耗时: 0 次外部检索，约 3 分钟。
- 产出: 边等级分布为实边184、半边槽位33、程序段落泄漏2、推断边(A级)2、推断边(B级)2、实边(已死亡)1、半边1；`grade_class()` 对所有 `推断边(...)` 已统一映射 `infer`。v1.4 节点点击会列出全部入边/出边，可直接保留。发现 `flows/out/v16-dsp-layer.md` 明确要求 DSP→模块具名边作为制度性盲区展示。
- 偏差: v1.4 分段输出仍从 CSS 中段开始，但已覆盖完整布局、交互、统计、页脚和自测代码；文件开头此前已读取到 `build()` 结束，基线信息完整。
- 摩擦: CSV 类型粒度不统一（55 种），其中设备、科研、物流、跨行业节点不完全落在用户给出的七个标准产业层名；需要明确且可断言覆盖的类型规则。

- 动作前记录：下一步读取 `flows/out/v16-dsp-layer.md`、161 节点完整名称/类型表、边端点对 nodes 的规范化缺口，以及 v1.4 前 48 节点的旧层与类型对照；据此设计仅按类型字段驱动且全覆盖的七层映射。

### [P4-X2-v1.5-03] 核对 DSP 判定、节点全集与旧层对照
- 意图: 为类型驱动七层映射建立全量依据，并识别基线读取风险。
- 动作: 读取 `flows/out/v16-dsp-layer.md`；打印 N01–N161 的名称/类型/国别；对照 N01–N48 的 v1.4 手工层；尝试用 v1.4 `build()` 解析当前 CSV；检索目标 HTML 的既有引用。
- 耗时: 0 次外部检索，约 3 分钟。
- 产出: DSP 盲区判定明确覆盖 Broadcom/Marvell；新节点类型已为七层提供多数直接标签。发现当前 CSV 含 UTF-8 BOM，v1.4 用 `encoding="utf-8"` 导致 `edge_id` 读成带 BOM 键并报 `KeyError`，因此 v1.5 必须用 `utf-8-sig`。确认输出报告已预留 `光模块产业图谱-v1.5.html` 链接。
- 偏差: 原计划的端点规范化缺口检查因 v1.4 BOM 兼容性失败未完成；将改用独立只读统计，不修改冻结脚本。
- 摩擦: `供应链主体` 等泛型无法单靠名称精确表达产业位置；为遵守“按类型自动归层”，需给泛型一个固定层并在断言中保证不漏层，而非为具体公司加手工例外。

- 动作前记录：下一步独立统计规范化后仍未命中 nodes 的端点，试算一套按类型关键字优先级驱动的七层分布，并读取目标报告邻近段落确认页脚判例口径。

### [P4-X2-v1.5-04] 审计端点缺口与判例徽章口径
- 意图: 区分真实匿名幽灵与可归一化的实名端点，并锁定页脚徽章文字。
- 动作: 复用 v1.4 `norm()` 独立核对 225 边端点；读取 `光模块产业结构-v1.5.md` 的判例分布与继续核对段；试算类型关键字映射分布。
- 耗时: 0 次外部检索，约 2 分钟。
- 产出: 规范化后有 22 个未命中端点、29 次引用；其中 21 个为匿名槽位，唯一实名别名缺口为 `Corning(解匿)`，应归一到 `Corning Incorporated`。报告明确徽章口径为 `225边/161节点/判例2A+2B`。
- 偏差: 首次试算类型分布的列表推导式只创建了未消费的生成器，未打印出计数；下一步用 `Counter` 重算并保留全覆盖断言。
- 摩擦: 无新增工具摩擦；发现一个需在 v1.5 修复的别名兼容点，但不改数据源。

- 动作前记录：下一步重算七层类型分布与兜底类型清单；随后新增独立 `build_atlas_v15.py`，采用 UTF-8 BOM 兼容读取、类型驱动七层、两行排布、DSP 盲区标记，并保留 v1.4 证据抽屉和边筛选交互。

### [P4-X2-v1.5-05] 新增 v1.5 独立构建器
- 意图: 在不触碰冻结 v1.4 的前提下实现 161 节点、225 边的七层披露证据地图。
- 动作: 新建 `flows/src/build_atlas_v15.py`；加入 UTF-8 BOM 兼容读取、按 `类型` 关键字优先级自动归层、七层纵向/层内最多两行布局、匿名端点幽灵槽位、`Corning(解匿)` 别名归一、五类边样式、节点全部入出边抽屉、DSP 盲区虚线标记与 `225边/161节点/判例2A+2B` 页脚徽章。
- 耗时: 0 次外部检索，约 12 分钟。
- 产出: `flows/src/build_atlas_v15.py`。
- 偏差: 为容纳用户指定七层中未单列的设备/科研类型，将设备、仪器、科研、代理、物流按类型规则并入第3层制造/委托加工带；`材料/设备供应商` 仍归第1层。匿名端点作为渲染幽灵，不计入 161 节点。
- 摩擦: 泛型 `供应链主体` 只能一致归入“模块/光电子商”层，无法在不违反按类型归层要求的情况下按公司名称细分。

- 动作前记录：下一步先用 Python `compile()` 做脚本语法自检，再运行构建器生成 v1.5 HTML；若断言失败，只修改 v1.5 新文件并重新测试。

### [P4-X2-v1.5-06] Python 语法检查与首次构建
- 意图: 验证构建脚本可执行并生成满足规模与分层约束的单文件 HTML。
- 动作: 用 Python `compile()` 检查 `build_atlas_v15.py`；运行构建器及其内置断言。
- 耗时: 0 次外部检索，约 1 分钟。
- 产出: 生成 `output/光模块产业图谱-v1.5.html`（112,458 bytes）。节点分层为 L1 26、L2 18、L3 33、L4 20、L5 39、L6 21、L7 4，合计 161；另有 21 个匿名幽灵槽位；225 边全部进入载荷。
- 偏差: 原端点缺口统计为 22 个，加入 Corning 别名归一后匿名幽灵为 21 个，符合预期。
- 摩擦: 无。

- 动作前记录：下一步用 Node 对生成 HTML 的内联 JavaScript 做语法检查；解析 JSON 载荷复核节点/边/七层/DSP/B级；检查外部资源标签、git diff 与 v1.4 冻结文件状态。

### [P4-X2-v1.5-07] 最终静态校验与冻结核对
- 意图: 在不启动浏览器的条件下验证 HTML 脚本、载荷、资源依赖和冻结边界。
- 动作: 用 Node `new Function()` 检查内联 JavaScript；解析内联 JSON；搜索外部脚本/样式资源引用；检查 v1.4 脚本与 HTML 的 git diff；运行差异空白检查。
- 耗时: 0 次外部检索，约 1 分钟。
- 产出: JavaScript 语法通过；载荷为 225 边、161 数据节点、21 幽灵槽位，边样式计数 184 实边/34 半边/4 推断/2 程序泄漏/1 死亡；DSP 标记恰为博通和 Marvell；徽章文本准确；无外部脚本/样式资源；v1.4 两个冻结文件无 diff。
- 偏差: 无。
- 摩擦: 无。

- 动作前记录：下一步只读查看 `logs/rehearsal-log.md` 末尾编号，并按主日志纪律追加本路收口索引；不伪装为事前记录，详细同时序日志仍以本文件为准。
