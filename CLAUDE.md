# CLAUDE.md — 重定向（不是政策文件）

**先读 `AGENTS.md`。** 它规定每次会话要读什么、禁止读什么、什么时候必须停下来升级。

读完 `AGENTS.md` 后：

- 做点 / 边 / 判定闸 / 语料台账工作 → 再读 `docs/policies/point-ledger.md`（点先行 v2，共 9 行）。
- 做其他任何工作 → 按 `AGENTS.md` 指向的 `docs/control/PROJECT_CHARTER.md` 与
  `docs/control/ACTIVE_WORKPACK.yaml` 执行。

本文件不再承载规则。点/边/判定/语料台账的九条纪律已完整迁至
`docs/policies/point-ledger.md`，规则与含义未变。
