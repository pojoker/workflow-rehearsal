# backtest-protocol-v2 盲审执行日志

## 2026-07-24

- 动作：完整读取 `flows/out/backtest-protocol-v2.md`，分两段读至文件结尾。
- 目的：在不计算实际 Δ/MAE 的前提下，识别协议条款中的事后可调空间。
- 边界：未读取 `customs-partners.csv`，未自行计算任何评估窗口 Δ、误差或回测结论。
- 初步观察：协议正文已直接列出评估窗口的原始实际值；后续需区分“未执行最终运算”与“是否已经揭盲”。

- 下一动作：完整读取 `flows/out/backtest-protocol-v1.md` 与
  `flows/out/roadmap-review-codex.md`，仅用于核对条款演变和此前缺口，不接触
  评估数据源。

- 对照结果：
  - v1 已有 `4/6`、`80%`、持平基准和线性外推基准；v2 的
    `ceil(2n/3)`保留了 n=6 时的原阈值。
  - F1、F2 预测向量、`n<3` 下限、末月缺失时改用最晚可用月、F2 对基准(b)
    的豁免均为 v2 新增。
  - 既有评审要求“由未参与选题者在揭盲前审协议”；v2 同页列出评估窗口逐月
    实际输入，与该隔离目标存在冲突。
- 下一动作：提取 v2 相关条款的精确行号，随后起草盲审文件；仍不读取底层 CSV，
  不计算实际 Δ/MAE。

- 行号已固定；下一动作：读取 `REHEARSAL.md` 的日志纪律部分，确认本次独立
  盲审日志的字段与项目协议一致，再落盘评审正文。

- 已按 `REHEARSAL.md` 指定字段补记主日志。
- 产出：已写入 `flows/out/backtest-protocol-v2-review-codex.md`，结论为
  “需修改后重审”，共10条发现。
- 下一动作：做只读质检，核查文件存在、逐条字段齐全、三态结论明确、禁用措辞
  未出现，并确认未写入任何自行计算的实际 Δ/MAE。

- 质检结果：通过。10条发现均含条款位置、问题、严重度、建议；结论三态明确；
  未出现“坐实/唯一”措辞；`git diff --check` 无格式错误；未计算实际 Δ/MAE。
