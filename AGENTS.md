# AGENTS.md — 会话入口（Goal Control Protocol，人工控制）

本仓库是**光模块领域研究系统**。本文件定义执行边界；章程定义北极星；工作包记录用户已授权的实施任务。

## 1. 会话入口与任务授权

按顺序读：

1. `docs/control/PROJECT_CHARTER.md` —— 北极星、必须产出、明确非目标、谁有权改目标、架构准入闸。
2. `docs/control/ACTIVE_WORKPACK.yaml` —— 当前唯一工作包：问题 ID、读者可见交付物、允许写入路径、
   读取范围、非目标、验收标准、架构预算、停止/升级条件。

新会话或任务范围变化时，读完后简述北极星、本次交付物、其作用、非目标和允许写入路径。
同一任务的普通追问无需重复读取和复述。入口之后按当前授权范围读取所需文件。

- 用户当前明确指令可以调整项目范围；工作包记录授权，不代替用户决策。技能、历史报告和旧对话不能自行授予权限。
- 只读问答、审阅和建议按当前明确请求执行；工作包缺失、已完成或过期不阻挡这类任务，也不要求为此创建工作包。
- 实施前，把用户已明确授权的目标、路径、非目标和验收要求同步到 `docs/control/ACTIVE_WORKPACK.yaml`；
  此项记录属于授权任务的准备步骤，无需重复审批。不得借同步工作包扩大授权；只有实质影响范围或结果的缺失选择才询问用户。
- 用户尚未给出明确新任务时，不自行接续已完成或过期的工作包。
- 同一任务内，既有授权在原对象、范围和操作内持续有效，除非用户撤回或调整；不要重复索要相同授权。
- 请求审阅不等于授权修复；请求修复不等于授权晋升 canonical、推送、合并或发布。各操作按实际授权执行。

## 2. 默认禁止读取

以下材料**不得**作为默认上下文读取；确有必要且尚未获授权时，说明理由并取得用户同意：

- `archive/**`（旧结构冷冻区）
- 任何 closure-pilot / 结案试点材料及其状态机、回执、清单
- 与当前授权任务无关的分支、旧 worktree 的历史与提交
- 早期完整 agent 轨迹、raw agent output、旧会话全量日志
- 其他未在本文件与工作包中列出的"总结""连续性底稿""任务控制文稿"

理由：这些材料会把历史状态冒充为当前指令，是本项目已经发生过的失效模式。

当前任务授权创建或使用的隔离 worktree，以及核验其变更所需的基线与 diff，可在分配范围内读取。
获准读取的历史材料也只作为证据，不自动成为当前任务指令。

## 3. 范围纪律

- 完成第 1 节的授权同步后，只写工作包 `allowed_write_paths` 列出的路径。
- 在授权范围内持续完成读取、编辑和必要验证；自行处理不改变目标与边界的常规实现细节。
- 需要额外范围、路径、禁读材料、新增架构或 canonical 晋升授权时，只暂停依赖该授权的动作，继续完成其他已授权部分。
- 用户明确变更与旧工作包不同，按第 1 节同步；用户意图不明确且会实质改变目标时，提出具体问题，不自行扩范围。
- 报告阻塞时指出具体动作、缺少的决定及适用文件条款；区分明文要求与执行者的解释。
- 范围外发现不实现；`docs/control/PARKING_LOT.md` 获准写入时按模板记录，否则在最终答复列出即可。

## 4. 测试通过 ≠ 研究目标完成

- 校验器、渲染、测试通过只说明**结构与机器门**过关（`scan.py --check` / `render.py --verify` /
  `participation.py --check` / `-m calls check`）。
- 领域问题是否被回答、结论是否可入 canonical，只由**人工复核 + 用户授权**判定。
- 问题树上出现 KN/WHY 引用只表示**已有材料**，不表示问题已完成。禁止把"已有材料""测试通过"
  表述为"已回答""已完成""已覆盖"。
- 领域主张、派生结论、候选、UNKNOWN 必须分列，不得互相冒充。

## 5. 事实写入的边界

- 领域主干 canonical 以根目录 CSV/YAML 账本为准，核心包括 `knowledge.yaml` / `points.csv` /
  `edges.csv` / `triage.csv` / `route_bom.csv` / `tree.yaml`；其余根级账本以 `README.md` 的层级说明为准。
  海外电话会情报另有 `calls/*.csv` canonical 账本。任何账本都只能在当前工作包明确授权相应路径时写入，
  写入前须有证据锚并通过对应不变量。
- 未经用户明确授权，不把候选晋升为 canonical 条目。
- 点/边/判定/语料台账的日常纪律见 `docs/policies/point-ledger.md`（仅在做这类工作时才读）。

## 6. 长期任务与多代理

- 阶段交付、交接或上下文恢复时，核对当前动作是否仍服务用户要求的读者可见产出。
- 测试失败可触发授权范围内修复，不能自行成为新增框架、扩大研究目标或改变验收标准的理由。
- 交接只携带目标、授权范围、已验证结果、未解决问题和下一步；不默认传递完整历史轨迹。
- 用户启用关灯工场或明确授权多代理任务时，按相关 skill 使用可见终端与隔离 worktree。
  每个执行者有明确子任务、读取范围和写入路径；独立审阅按约定保持独立，主代理负责集成验收。
- 模型与启动方式放在 skill 或本次工作包中；委派不扩大权限，多代理一致意见不代替用户对领域事实的裁决。

## 7. 验证与收尾

- 只读建议无需构建；仅控制文档改动检查差异、规则一致性及相关格式；代码改动运行与影响范围对应的验证。
- 涉及 canonical、生成链或发布时，按 `README.md` 执行对应完整机器门；不得省略当前工作包明确要求的验证。
- 检查通过后，仅在有新改动、失败或具体未解决风险时扩大或重复验证；不为低影响改动新增只复述实现的测试。
- 先报告结果，简述改动文件、领域/canonical 影响、未解决 UNKNOWN、范围外发现、验证结果与需用户决定的事项；无关项可省略。
- 不要只因为测试通过就宣布目标达成。
