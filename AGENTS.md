# AGENTS.md — 会话入口（Goal Control Protocol，人工控制）

本仓库是**光模块领域研究系统**。任何会话（含全新上下文的执行者）开始工作前，按固定顺序读三个文件，
不要自行扩大阅读范围。

## 1. 每次会话开始：必读且只读这两份

按顺序读：

1. `docs/control/PROJECT_CHARTER.md` —— 北极星、必须产出、明确非目标、谁有权改目标、架构准入闸。
2. `docs/control/ACTIVE_WORKPACK.yaml` —— 当前唯一工作包：问题 ID、读者可见交付物、允许写入路径、
   读取范围、非目标、验收标准、架构预算、停止/升级条件。

读完必须**先复述**再动手：北极星、本次交付物、为什么推进北极星、非目标、允许写入路径。
复述与工作包不一致时，停下来问用户，不要"边做边猜"。

没有 `ACTIVE_WORKPACK.yaml`，或工作包已过期/与用户指令冲突：**停止并向用户升级**，不要自己另立任务。

## 2. 默认禁止读取

以下材料**不得**作为默认上下文读取；确有必要须先向用户说明理由并取得同意：

- `archive/**`（旧结构冷冻区）
- 任何 closure-pilot / 结案试点材料及其状态机、回执、清单
- 其他分支、其他 worktree 的历史与提交
- 早期完整 agent 轨迹、raw agent output、旧会话全量日志
- 其他未在本文件与工作包中列出的"总结""连续性底稿""任务控制文稿"

理由：这些材料会把历史状态冒充为当前指令，是本项目已经发生过的失效模式。

## 3. 范围纪律

- 只写工作包 `allowed_write_paths` 列出的路径。写别的路径前先停并升级。
- 遇到下列任一情况，**停止并升级**，不要自行扩大范围：
  - 要回答的问题不在工作包问题列表内；
  - 需要的写入路径不在允许列表内；
  - 需要新增架构（新关系类型、新顶层体系、新 schema、新引擎、新状态机）；
  - 发现必须引用被禁读材料才能继续；
  - 用户指令与工作包冲突；
  - 要把候选结论写成 canonical 事实，但没有用户明确授权。
- 工作包之外的新发现，写进 `docs/control/PARKING_LOT.md` 的模板条目，**不实现**。

## 4. 测试通过 ≠ 研究目标完成

- 校验器、渲染、测试通过只说明**结构与机器门**过关（`scan.py --check` / `render.py --verify` /
  `participation.py --check` / `-m calls check`）。
- 领域问题是否被回答、结论是否可入 canonical，只由**人工复核 + 用户授权**判定。
- 问题树上出现 KN/WHY 引用只表示**已有材料**，不表示问题已完成。禁止把"已有材料""测试通过"
  表述为"已回答""已完成""已覆盖"。
- 领域主张、派生结论、候选、UNKNOWN 必须分列，不得互相冒充。

## 5. 事实写入的边界

- 领域主干 canonical 以根目录 CSV/YAML 账本为准，核心包括 `knowledge.yaml` / `points.csv` /
  `edges.csv` / `triage.csv` / `route_bom.csv` / `tree.yaml`；其余根级账本以 `README.md` 的层级说明为准。
  海外电话会情报另有 `calls/*.csv` canonical 账本。任何账本都只能在当前工作包明确授权相应路径时写入，
  写入前须有证据锚并通过对应不变量。
- 未经用户明确授权，不把候选晋升为 canonical 条目。
- 点/边/判定/语料台账的日常纪律见 `docs/policies/point-ledger.md`（仅在做这类工作时才读）。

## 6. 收尾

- 会话末按 `README.md` 的校验命令跑一遍机器门。
- 报告：改动文件、领域/canonical 影响、未解决 UNKNOWN、范围外发现、测试结果、停止/升级项。
- 不要只因为测试通过就宣布目标达成。
