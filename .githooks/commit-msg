#!/bin/bash
head -1 "$1" | grep -qE '^(产出: .*\+[0-9]+点 \+[0-9]+边 空格[0-9]+/[0-9]+ 驳回[0-9]+|维护:)' || {
  echo "commit-msg拒绝: 首行须为 '产出: +N点 +M边 空格A/B 驳回K ...' 或 '维护: ...'"
  echo "  空格A/B=当前空叶格/总格数,跑 python3 render.py 看页脚(纪律第8条)"; exit 1; }
exit 0
