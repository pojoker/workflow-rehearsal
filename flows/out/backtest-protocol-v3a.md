# flows/out/backtest-protocol-v3a.md — v3 修订条款（依 codex 复审最低修改清单，2026-07-24）

**地位**：本文件与 `backtest-protocol-v3.md` 合并构成生效协议；冲突处以本文件
为准。v3 原文不改动。修订依据：`flows/out/backtest-protocol-v3-review-codex.md`
（结论"需修改后重审"，最低修改清单 7 项，本文件逐项落实）。撰写时点声明：
2026-07 及以后任何评估月实际值仍不存在于本项目与任何官方发布物（v3 F.1 的
不存在性证明与 codex 复审的独立核验均在案），本文件全部修订在目标值产生前
写定。

## 修订1（对应清单1）：信息集与建模回看窗口分层 + 候选集重定义

**1a. 可用信息集**：T0 时刻库内 `customs-partners.csv` 全部数据 =
2025-01～2026-06 共 18 个月；其中 17 个月美国/马来西亚双行可观测，
**2025-12 美国行缺失**（v2 3.6 已登记），该月 Δ 不可观测。v3 F.1 与 F.3
之间"18 个月信息集 vs 只用 6 个月建模"的未声明落差（codex 发现3）由本条
显式分层修复。

**1b. 建模回看窗口 L**：冻结为**预测窗口起点前最近 6 个可观测月**
（2026-01～2026-06）。来源登记（并入 v3 F.8 阈值登记表，同一框架）：

| 阈值 | 数值 | 独立外部依据 | 污点登记 |
|---|---|---|---|
| 建模回看窗口 L | 最近 6 个可观测月 | **无**——"6"与"18"或其他长度之间不存在机械必然性，任何 L 都是约定 | **选定时协议作者已见全部 18 个月历史**（含 2025 年 Δ 负转正的结构变化），不能排除 L 的选择受已见历史影响；但**对 2026H2 目标值无泄漏**（目标值物理不存在）。解释限制：分支 F 结果仅对"L=6 的这组固定规则"成立，不得外推为"任意窗口下均成立" |

codex 复审发现3 指出"窗口自由度上移一层并未消失"——本条不再声称消除该
自由度，改为按 codex 发现7 部分接受所确立的同一框架处理：**预注册的任意
阈值可以执行，污点如实登记，解释受限**。codex 复审正文已明示接受
"若 v3a 把 L 固定为 2026-01～06"这一路径。

**1c. 删除"穷尽"断言**：v3 F.3"四者穷尽最小完备族/不存在剩余自由度"的
表述**作废**。替代表述：候选集的封闭性来自**预注册**（本文件冻结后不得
增删），不来自逻辑穷尽。

**1d. 候选集（冻结，全跑全报）**：

- **基准（不参与裁决）**：F0 朴素持平 = 198,829,029；F2 OLS（L 上 6 点
  拟合）= v3 F.3 表原值（219,511,941 → 248,067,990）。
- **待裁决候选（3 个，各自独立过门）**：
  - F1 三月均值 = 219,460,481（窗口 = L 的一半，登记为约定+同 1b 污点框架）
  - F1b 六月均值 = 199,522,708（窗口 = L 全长）
  - **F3 全窗口中位数**（盲审人 codex 在盲审阶段提出，按协议邀请纳入）：
    `F3_pred = median(Δ_2026-01..Δ_2026-06)`，L 内六值排序后取中两值均值 =
    (194,000,663 + 198,829,029) / 2 = **196,414,846**（精确整除，无舍入），
    t=7..12 恒为该常数。
- **F3 门1 判定（T0 补登记，不依赖任何未来数据）**：方向 6/6 月 Δ_pred>0
  ≥4 ✓；幅度 2026-12 预测 196,414,846 ≥ 159,063,223.2 ✓ → **门1 通过**。
  至此三个待裁决候选（F1/F1b/F3）门1 全部通过，**弃权数 = 0**。
- **弃权披露规则（通用化，codex 发现8 建议采纳）**：门1 短路正式命名为
  "候选资格/弃权状态"；结果文件必须报告候选总数、过门数、弃权数三个
  计数，禁止只报过门者。

## 修订2（对应清单2）：分支 H 措辞降格

v3 头部与 H.0/H.6 中"证明计算管线可以机械算通/验证计算管线本身没有 bug"
的表述**作废**，统一替换为：**"检查本固定夹具能否按预定公式生成可复核
输出"**；并补充：**该检查不证明实现无 bug、不证明其他输入可运行、不证明
流程有效**。一次固定输入的烟雾测试只能发现部分故障或证明该路径在该输入上
产生输出（codex 新发现11 措辞，逐字采纳）。

## 修订3（对应清单3）：80% 污点表述改写

v3 F.8 中"污点不随复用转移到本次应用"的字面论证**作废**，替换为 codex
发现7 复审指定措辞：**"分支 F 没有对 2026H2 目标值的新增泄漏；80% 的历史
选择污点继续附着于规则来源，限制其一般化解释，但不破坏本次前瞻结果盲态。"**
被旧样本调过的阈值可以做真正前瞻检验，但它仍是"在旧样本开发、在新样本
验证"的规则，不是无开发史的先验阈值。

## 修订4（对应清单4）：决策表收紧为候选级五行表 + 禁止分支级聚合

v3 F.6 六格表**作废**（门2"数据不足"时门3 不计算，原表两个不可达格），
替换为**候选级三态表**（对每个已过门1 的候选独立执行）：

| # | 门2（实际事件判定） | 门3（增量价值） | 候选级三态结果 |
|---|---|---|---|
| 1 | 数据不足（n<3 或 2026-12 缺失） | **不计算** | 数据不足 |
| 2 | 未命中（n≥3，方向或幅度未同时达标） | 优于两基准 | 失败（禁止把低 MAE 单独包装成成功） |
| 3 | 未命中 | 不优于两基准 | 失败 |
| 4 | 命中 | 优于两基准 | 成功 |
| 5 | 命中 | 不优于两基准 | 失败（禁止把方向蒙对单独包装成成功） |

**只有候选级结果，没有分支级成功/失败**：最终结果 = 候选结果向量
（F1, F1b, F3 各一格 + 弃权计数），必须完整报告；**明确禁止**从多个候选中
摘出成功者包装成"分支 F 成功"或"回测成功"的事后聚合表述。

## 修订5（对应清单5）：评估用预测向量与精度写死

- 评估使用的**唯一真值向量** = 整数 USD 向量：F0 恒 198,829,029；F1 恒
  219,460,481；F1b 恒 199,522,708；F3 恒 196,414,846；F2 =
  (219,511,941, 225,223,151, 230,934,361, 236,645,570, 242,356,780,
  248,067,990)。取整规则：四舍五入到整数 USD（F3 精确整除无舍入）。
  评估执行时**不得重拟合、不得改用更高精度系数版本**。
- 门3 严格比较的执行方式：因各候选/基准共享同一可观测月集合（n 相同），
  `MAE_X < MAE_Y ⇔ Σ|Pred_X−Actual| < Σ|Pred_Y−Actual|`——**直接比较整数
  绝对差之和**，不做除法，无浮点精度歧义（codex 新发现14 闭合）。

## 修订6（对应清单6）：删除揭盲后澄清权，写死版本适用边界

v3 F.9.5**作废**，替换为：

1. **版本适用边界**：自任何参与者看到首个目标月（2026-07）实际值起，任何
   后续协议版本（v3b、v4…）**只对下一轮未来试验生效，不得回用于 2026H2
   窗口**。
2. 若执行时发现本协议（v3+v3a 合并文本）存在影响裁决的歧义：该候选该项
   判定直接判**"协议不可判定/数据不足"**，不得由已见目标值的作者补规则
   解释，也不得临时指定裁决人。
3. 唯一例外通道：在 2026-07 批次入库**之前**（即所有参与者仍未见任何目标
   值时）发现的歧义，可出 v3b 修订并重新交 codex 复审，性质与本 v3a 相同。

## 修订7（对应清单7）：commit 冻结锚点

- 本文件与 v3 在 codex 复审通过后**立即提交 git**（在任何 2026-07 实际值
  入库之前），首次提交的 commit hash 即协议冻结锚点。
- **T0 重定义为该不可变提交时刻**（若提交日仍为 2026-07-24 则 T0 日期
  不变；跨日则按 codex 要求同步改日期并重验信息集不存在性）。
- v3 F.9.4 的 `<COMMIT_HASH_PLACEHOLDER>` 语义变更：结果文件引用的是
  **git log 中 v3+v3a 首次落盘 commit 的 hash**（提交后可由 `git log
  --follow` 审计取得），本文件不再以占位符文本冒充冻结证据。
- 依 codex 复审终条：**v3a 复审通过并完成不可变冻结之前，分支 H 不得开始
  烟雾测试，分支 F 不得执行门2/门3**；通过并冻结后，分支 H 烟雾测试可
  执行，分支 F 逐月留痕等待 2026-07～12 批次，完整判定不早于 2027-01。

---

**状态重申**：本文件在 2026H2 任何目标值产生前写定；不预告任何候选成功；
候选结果向量未来无论如何分布，均仅证明"预注册流程能否在真未来窗口走通"，
不得表述为图谱预测力证据。
