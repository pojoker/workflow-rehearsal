# raw-output-tq004-attempt2 勘误

raw 保留为 Pi 原始输出，不直接修改。以下内容已废止：

1. raw §3 中“pluggable vs CPO 粒度错层”改读为：`pluggable / NPO / CPO` 可在广义
   放置轴比较；真正错层是 `OSFP/QSFP-DD vs CPO`。
2. raw §8 对基础合同 §3.3 的“符合”只表示 Pi 遵守了当时合同，不代表该合同条款正确；
   `adjudication-tq004-final.md` 已正式改正该条款。
3. raw 中 `LPO = linear + pluggable + LPO MSA v1.2 profile` 不得作 LPO 泛称的全称定义。
   有效口径为：泛称 `LPO = linear + pluggable`；本轮冻结的具体 profile 实例是
   `100G-DR-LPO Revision 1.0（下载文件名含 v1p2）`。
4. raw 中 `on-board/NPO` 不得合并为一个轴值；两者在 TQ008 中分别记录。
