# PQ002 attempt-3 最终修订合同

继承前两轮合同与裁决，只做最终结构收敛：

1. 输出完整但紧凑的最终候选稿；原子主张最多 8 条。
2. 每条在字段体中显式写唯一 `draft_id: PQ002-a3-dNN`，并保留其余全部字段。
3. CMIS Application 只写 host-side/media-side 间 bridge/forwarding，不自行补“到达远端并返回”。
4. 必须保留正确的 engine-to-substrate attach / media-side connector 分离。
5. 保留 2 条挂 PQ004/PQ005 的证据触发注记，不新增问题 ID。
6. 只输出最终候选稿，不复述修订过程。
